# API/Socket base URL (used at build time for Vite)
# Dev: defaults to http://localhost:3001
# Production: set to your backend URL, e.g. http://your-vm-ip:3001
# If omitted in production, assumes same-origin (reverse proxy)
VITE_API_BASE=http://localhost:3001
