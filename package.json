{
  "name": "siam-smile-pos",
  "version": "1.0.0",
  "description": "Production-grade POS system for restaurants and cafes",
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "node scripts/dev.js",
    "build": "cd frontend && npm run build && cd .. && node scripts/copy-build.js",
    "start": "cd backend && npm start",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:stop": "pm2 stop ecosystem.config.cjs",
    "pm2:restart": "pm2 restart ecosystem.config.cjs",
    "pm2:logs": "pm2 logs siam-smile-pos"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["pos", "restaurant", "ordering", "real-time"],
  "license": "MIT"
}
